import numpy as np
# from lab3_task1 import g
from lab3_task1 import e
from lab3_task1 import k
from lab3_task1 import hp
from lab3_task1 import pi

# h = 100;
# alpha = pi;
# beta = pi / 6;
T = 200
E = 300

# v = ( (g * h * np.tan(beta) ** 2) / (2 * np.cos(alpha) ** 2 * (1 - np.tan(alpha) * np.tan(beta))) ) ** (1 / 2);
# print(v);

# N = ( 2 / (pi ** 0.5) ) * ( hp / ( (k * T) ** (1.5) ) ) * e ** (- E / (k * T) ) * E ** (T / 2)
print(N)